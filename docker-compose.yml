version: "3.9"

services:
  wikidot-api:
    build: .
    container_name: wikidot-api
    ports:
      - "3030:3030"
    environment:
      - PORT=3030
      - WIKIDOT_SITE=mc-anomaly-archives
    restart: unless-stopped
